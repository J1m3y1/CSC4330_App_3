{
  "name": "fantasi-web-app",
  "private": true,
  "version": "1.0.0",
  "description": "Azure App Service deployment entry point for Fantasi",
  "scripts": {
    "start": "node server.js",
    "postinstall": "npm --prefix backend ci --omit=dev"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
